#!/bin/bash
#BSUB -q {queue}
#BSUB -W {walltime}
#BSUB -J {jobname}
#BSUB -P acc_MMEDS
#BSUB -n {nodes}
#BSUB -R "span[hosts=1]"
#BSUB -R rusage[mem={memory}]
#BSUB -o {path}/{jobname}.stdout
#BSUB -eo {path}/{jobname}.stderr
#BSUB -L /bin/bash

export QIIME_BSUB_OPTIONS='-q {queue} -P acc_MMEDS -W {walltime2} -n 1 -R rusage[mem=2000]';
module use /hpc/packages/minerva-common/modulefiles
